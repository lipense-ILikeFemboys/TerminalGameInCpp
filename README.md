# TerminalGameInCpp

the project's title says everything, u just need to put this code on a C++ compiler, who runs a terminal, and see the MOST BEAUTIFUL THING u will ever see (until my next code uploaded). 

Joking away, this code runs a game in a cpp terminal, thats right. Nothing, but a C++ 11 or above compiler and, of course, the CODE is needed for the "game", if we can call it a game.

If im not crazy, its just 69 lines (no worrying with lag!), and the code is SUPER EASY to change anything if u want (again, if im not crazy. Im checking right now, and, all the most important variables, who determines the size of map... player's step .. players position... ill say them:

int x = 25 > the player starts on the 25 in position X.
int y = 11 > eleven in position Y!

char ambientSymbol = '*' > everything, that is not the player (and its not something u could add), is made by "*".

char playerSymbol = 'O' > And for u understand, one only character for each 'char'. Btw, this is the character that represents the PLAYER!

int moveSpeed = 1 > In any direction, player will move 1 step.

int screenSize = 75 > This is the map width. 75 = 75 characters.

int ylimit = 20 > The HEIGHT of the map!